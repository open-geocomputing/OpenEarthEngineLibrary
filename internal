exports.addRef=function(name,ref){exports.ref.push(ref)};

exports.setupLibrary=function(lib,keys){
  print(lib)
  print(keys)
  for(var key in keys){
    print(key)
    //lib[k].internal=null;//lib.internal;
  }
};

